import { LandingPage } from "@/components/landing-page";

export default function Home() {
    return (
        <main className="flex min-h-screen w-full bg-transparent transition-colors flex-col items-center">
            <LandingPage />
        </main>
    );
}
